"use client";

import FeaturePage from "../components/FeaturePage";

export default function ClubsPage() {
  return (
    <FeaturePage
      title="Clubs"
      subtitle="Keep your clubs, memberships, invites, and collaboration spaces in one place."
      label="Club Hub"
      actionLabel="Find Clubs"
      emptyTitle="No clubs yet"
      emptyDescription="Club memberships and invitations will appear here after clubs are added to EventiX."
    />
  );
}
